{
  "buildCommand": "pnpm install && vite build && mkdir -p api && esbuild server/vercel-entry.ts --bundle --platform=node --format=cjs --packages=external --outfile=api/index.js --alias:@shared=./shared --alias:@=./client/src",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
