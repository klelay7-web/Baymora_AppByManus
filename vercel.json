{
  "buildCommand": "pnpm install && vite build && esbuild api/index.ts --bundle --platform=node --format=esm --packages=external --outdir=api --out-extension:.js=.mjs --allow-overwrite --alias:@shared=./shared",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.mjs" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
